services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "3000:80"
    labels:
      - "com.portfolio.service=web"
      - "com.portfolio.version=1.0"
      
